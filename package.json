{
  "name": "my-cql-project",
  "version": "1.0.0",
  "scripts": {
    "test": "./node_modules/.bin/mocha --reporter spec --recursive",
    "cql-to-elm": "cross-env ./gradlew cql2elm"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "cql-testing": "^2.0.0",
    "cql-execution": "^3.0.0",
    "cross-env": "^7.0.3"
  }
}